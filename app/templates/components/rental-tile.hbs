<li class="list-group-item">
  {{#link-to 'rental' rental.id}}{{rental.owner}}'s {{rental.type}} in {{rental.city}} {{/link-to}}
  {{#if isImageShowing}}
  <p><img class="img-thumbnal" src={{rental.image}} alt={{rental.type}} {{action 'imageHide'}} width="400px"></p>
  {{else}}
    <button class="button btn-info pull-right"{{action 'imageShow'}}>Show image</button>
  {{/if}}
</li>
